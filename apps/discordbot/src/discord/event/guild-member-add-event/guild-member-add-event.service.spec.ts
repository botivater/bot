import { Test, TestingModule } from '@nestjs/testing';
import { GuildMemberAddEventService } from './guild-member-add-event.service';

describe('GuildMemberAddEventService', () => {
  let service: GuildMemberAddEventService;

  beforeEach(async () => {
    // const module: TestingModule = await Test.createTestingModule({
    //   providers: [GuildMemberAddEventService],
    // }).compile();
    // service = module.get<GuildMemberAddEventService>(
    //   GuildMemberAddEventService,
    // );
  });

  it('should be defined', () => {
    // expect(service).toBeDefined();
  });
});
